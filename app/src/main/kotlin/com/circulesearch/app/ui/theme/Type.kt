package com.circulesearch.app.ui.theme

import androidx.compose.material3.Typography

/** Platform default type ramp — no custom font family needed for v1. */
val CircleSearchTypography = Typography()
